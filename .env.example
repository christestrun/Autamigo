# This file serves as an example of the environment variables required by the project.
# To set up your local environment, create a copy of this file and name it .env.
# Then, replace the placeholder values with your actual test credentials.
#
# The .env file is intentionally excluded from version control via .gitignore.

PW_USERNAME=username
PW_PASSWORD=password